#!/usr/bin/env python
"""Initial Searchat setup helper.

This compatibility wrapper runs the interactive setup wizard.
After setup, start `searchat-web` and the server will perform startup catch-up indexing.
"""

from searchat.setup.wizard import main

if __name__ == "__main__":
    main()
